package zio.blocks.schema

// This file is intentionally left minimal in the shared `src/main/scala`
// directory. Platform/version-specific implementations are provided in
// `scala-2/` and `scala-3/` directories to keep the cross-builds clean.
