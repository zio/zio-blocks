package zio.blocks.schema

/**
 * Placeholder for Scala 2 - markdown schemas are not available.
 *
 * Scala 2's macro implementation cannot derive schemas for types from external
 * modules in the same compilation run, so these instances are only available in
 * Scala 3.
 *
 * This file exists to maintain source compatibility and prevent compilation
 * errors in Scala 2 builds.
 */
private[schema] trait DocsSchemas
