package zio.blocks.schema

// This file is intentionally empty.
// The annotations (optic, optic_, deriveSchema) were never introduced in any
// released version of ZIO Schema, so they are not included here.
// Use `extends DerivedOptics[T]` pattern for optics derivation instead.
