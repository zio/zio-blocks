package zio.blocks

/**
 * Scala 3 provides native AnyKind support.
 * We re-export it for consistency across the codebase.
 */
package object typeid {
  // AnyKind is available natively in Scala 3
}
